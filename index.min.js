'use strict';const jsonServer=require("json-server"),cors=require("cors"),path=require("path"),server=jsonServer.create(),router=jsonServer.router(path.join(__dirname,"db.json")),middlewares=jsonServer.defaults();server.use(cors());server.use(jsonServer.bodyParser);server.use(middlewares);server.use(router);const PORT=8E3;server.listen(PORT,()=>{console.log(`JSON Server is running on http://localhost:${PORT}`)});